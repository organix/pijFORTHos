
MEMORY
{
    arm : ORIGIN = 0x8000, LENGTH = 0x20000
}

SECTIONS
{
    .text : { *(.text*) } > arm
	_etext = .;
    .rodata : { *(.rodata*) } > arm
    .data : { *(.data*) } > arm
    .bss : { *(.bss*) } > arm
}

